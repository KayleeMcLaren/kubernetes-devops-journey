apiVersion: v1
kind: Service
metadata:
  name: dynamodb-local
  labels:
    app: dynamodb-local
spec:
  selector:
    app: dynamodb-local
  ports:
  - protocol: TCP
    port: 8000
    targetPort: 8000
    name: dynamodb
  type: ClusterIP